invoke >= 0.20.2
markdown
pymdown-extensions
minchin.pelican.plugins.autoloader >= 1.2.4
minchin.pelican.plugins.cname >= 1.2.0
minchin.pelican.plugins.nojekyll
minchin.pelican.plugins.optimize_images
pelican >= 3.7
pelican-extended-sitemap
# pelican-minify  # causes issues with jquery
pip-tools
seafoam >= 2.15.1
typogrify
# process site using HTML5
html5lib

# sub-dependencies; minimums for security
pillow >= 10.0.1
jinja2 >= 3.1.6

# required for Ubuntu (GitHub Actions) builds
pytz #; sys_platform=="linux"
